source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in webpacker-electron.gemspec.
gemspec

gem 'sqlite3', group: :development
# To use a debugger
# gem 'byebug', group: [:development, :test]
